diff --git a/LibraryCore/SystemModels/MagicInfo.cs b/LibraryCore/SystemModels/MagicInfo.cs
--- a/LibraryCore/SystemModels/MagicInfo.cs
+++ b/LibraryCore/SystemModels/MagicInfo.cs
@@ -202,6 +202,22 @@ namespace Library.SystemModels
         }
         private int _Delay;
 
+        public int LevelDelayReduction
+        {
+            get { return _LevelDelayReduction; }
+            set
+            {
+                if (_LevelDelayReduction == value) return;
+
+                var oldValue = _LevelDelayReduction;
+                _LevelDelayReduction = value;
+
+                OnChanged(oldValue, value, "LevelDelayReduction");
+            }
+        }
+        private int _LevelDelayReduction;
+
         public string Description
         {
             get { return _Description; }
diff --git a/ServerLibrary/Models/MagicObject.cs b/ServerLibrary/Models/MagicObject.cs
--- a/ServerLibrary/Models/MagicObject.cs
+++ b/ServerLibrary/Models/MagicObject.cs
@@ -303,7 +303,10 @@ namespace Server.Models
             }
 
             var cooldownMagic = magic ?? Magic;
-            var cooldownDelay = delayInMilliseconds ?? cooldownMagic.Info.Delay;
+            var cooldownDelay = delayInMilliseconds ?? Math.Max(
+                0,
+                cooldownMagic.Info.Delay - cooldownMagic.Level * cooldownMagic.Info.LevelDelayReduction);
 
             cooldownMagic.Cooldown = SEnvir.Now.AddMilliseconds(cooldownDelay);
             Player.Enqueue(new S.MagicCooldown { InfoIndex = cooldownMagic.Info.Index, Delay = cooldownDelay });
