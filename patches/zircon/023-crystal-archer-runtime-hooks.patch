diff --git a/LibraryCore/Enum.cs b/LibraryCore/Enum.cs
--- a/LibraryCore/Enum.cs
+++ b/LibraryCore/Enum.cs
@@
         MoonMist = 605,
+
+        // ORIGINS Crystal Archer runtime state/buffs.
+        ArcherConcentration = 620,
+        CrystalElementalBarrier = 621,
+        CrystalElementalBarrier1 = 622,
+        CrystalVampireShot = 623,
+        CrystalPoisonShot = 624,
+        CrystalOneWithNature = 625,
+        CrystalDelayedExplosion = 626,
+        CrystalDelayedExplosion2 = 627,
@@
         CrystalHealingCircle2,
+        CrystalExplosiveTrap,
+        CrystalNapalmShot,
+        CrystalNapalmShot2,
     }
diff --git a/LibraryCore/Stat.cs b/LibraryCore/Stat.cs
--- a/LibraryCore/Stat.cs
+++ b/LibraryCore/Stat.cs
@@
         HealingCircle2ReductionPercent = 11005,
+
+        [StatDescription(Title = "Crystal Archer Barrier Reduction", Format = "{0:+#0%;-#0%;#0%}", Mode = StatType.Percent, ServerOnly = true)]
+        CrystalArcherBarrierReductionPercent = 11006,
+
+        [StatDescription(Title = "Crystal Delayed Explosion Power", Format = "{0:+#0;-#0;#0}", Mode = StatType.Default, ServerOnly = true)]
+        CrystalDelayedExplosionPower = 11007,
+
+        [StatDescription(Title = "Crystal Delayed Explosion Infect", Format = "{0:+#0;-#0;#0}", Mode = StatType.Default, ServerOnly = true)]
+        CrystalDelayedExplosionInfect = 11008,
 
         [StatDescription(Title = "Duration", Mode = StatType.Time)]
diff --git a/ServerLibrary/Models/PlayerObject.cs b/ServerLibrary/Models/PlayerObject.cs
--- a/ServerLibrary/Models/PlayerObject.cs
+++ b/ServerLibrary/Models/PlayerObject.cs
@@
     public sealed partial class PlayerObject : MapObject
     {
+        // Crystal Archer runtime state.  The original stores MentalState and
+        // element progress on CharacterInfo; ORIGINS keeps this in Zircon's
+        // runtime object until the database persistence pass is compiled.
+        public byte CrystalArcherMentalState;
+        public byte CrystalArcherMentalStateLevel;
+        public int CrystalArcherElementsLevel;
+
+        public int CrystalArcherOrbCount
+        {
+            get
+            {
+                if (CrystalArcherElementsLevel >= 200) return 4;
+                if (CrystalArcherElementsLevel >= 150) return 3;
+                if (CrystalArcherElementsLevel >= 100) return 2;
+                if (CrystalArcherElementsLevel >= 50) return 1;
+                return 0;
+            }
+        }
+
+        public void CrystalArcherSeedElement(byte meditationLevel)
+        {
+            if (CrystalArcherElementsLevel <= 0)
+                CrystalArcherElementsLevel = meditationLevel >= 3 ? 100 : 50;
+        }
+
+        public void CrystalArcherGatherElement(byte meditationLevel)
+        {
+            CrystalArcherElementsLevel = Math.Min(200, CrystalArcherElementsLevel + 50);
+
+            // Crystal GatherOrbsPerLevel caps the available tier at the
+            // learned Meditation level. Level 3 can hold the fourth tier.
+            int cap = meditationLevel >= 3 ? 200 : 50 * (meditationLevel + 1);
+            CrystalArcherElementsLevel = Math.Min(CrystalArcherElementsLevel, cap);
+        }
@@
             if (Stats[Stat.HealingCircle2ReductionPercent] > 0)
             {
                 power -= power * Stats[Stat.HealingCircle2ReductionPercent] / 100;
             }
+
+            BuffInfo archerBarrier = Buffs.FirstOrDefault(x => x.Type == BuffType.CrystalElementalBarrier);
+            if (archerBarrier != null)
+            {
+                int reduction = Math.Max(0, archerBarrier.Stats[Stat.CrystalArcherBarrierReductionPercent]);
+                power -= power * reduction / 100;
+
+                // Crystal base ElementalBarrier burns 60 ms of remaining
+                // duration for every point that reaches the barrier.
+                archerBarrier.RemainingTime -= TimeSpan.FromMilliseconds(Math.Max(0, power) * 60L);
+                if (archerBarrier.RemainingTime <= TimeSpan.Zero)
+                    BuffRemove(archerBarrier);
+            }
+
+            BuffInfo archerBarrier1 = Buffs.FirstOrDefault(x => x.Type == BuffType.CrystalElementalBarrier1);
+            if (archerBarrier1 != null)
+            {
+                int reduction = Math.Max(0, archerBarrier1.Stats[Stat.CrystalArcherBarrierReductionPercent]);
+                power -= power * reduction / 100;
+
+                // Jev ElementalBarrier1 loses a flat three seconds per hit.
+                archerBarrier1.RemainingTime -= TimeSpan.FromSeconds(3);
+                if (archerBarrier1.RemainingTime <= TimeSpan.Zero)
+                    BuffRemove(archerBarrier1);
+            }
 
             if (element != Element.None)
diff --git a/ServerLibrary/Models/MapObject.cs b/ServerLibrary/Models/MapObject.cs
--- a/ServerLibrary/Models/MapObject.cs
+++ b/ServerLibrary/Models/MapObject.cs
@@
                     case BuffType.CrystalHealing:
@@
                         break;
+                    case BuffType.CrystalDelayedExplosion:
+                    case BuffType.CrystalDelayedExplosion2:
+                        buff.TickTime -= ticks;
+                        if (buff.TickTime > TimeSpan.Zero) continue;
+
+                        if (buff.Caster is PlayerObject explosionOwner && Node != null && !Dead)
+                        {
+                            MagicType explosionType = buff.Type == BuffType.CrystalDelayedExplosion2
+                                ? MagicType.DelayedExplosion2
+                                : MagicType.DelayedExplosion;
+
+                            explosionOwner.MagicAttack(
+                                new List<MagicType> { explosionType },
+                                this,
+                                true,
+                                null,
+                                buff.Stats[Stat.CrystalDelayedExplosionPower]);
+
+                            if (buff.Stats[Stat.CrystalDelayedExplosionInfect] > 0 && CurrentCell?.Objects != null)
+                            {
+                                // Jev's Infect flag allows the timed blast to seed nearby
+                                // hostiles. Keep the spread local and source-owned.
+                                foreach (MapObject nearby in CurrentCell.Objects.ToList())
+                                {
+                                    if (nearby == this || nearby.Node == null || !explosionOwner.CanAttackTarget(nearby)) continue;
+                                    if (nearby.Buffs.Any(x => x.Type == BuffType.CrystalDelayedExplosion2)) continue;
+
+                                    nearby.BuffAdd(
+                                        BuffType.CrystalDelayedExplosion2,
+                                        TimeSpan.FromSeconds(6),
+                                        new Stats
+                                        {
+                                            [Stat.CrystalDelayedExplosionPower] = buff.Stats[Stat.CrystalDelayedExplosionPower],
+                                            [Stat.CrystalDelayedExplosionInfect] = 0,
+                                        },
+                                        false,
+                                        false,
+                                        TimeSpan.FromMilliseconds(1500));
+                                }
+                            }
+                        }
+
+                        expiredBuffs.Add(buff);
+                        break;
diff --git a/ServerLibrary/Models/SpellObject.cs b/ServerLibrary/Models/SpellObject.cs
--- a/ServerLibrary/Models/SpellObject.cs
+++ b/ServerLibrary/Models/SpellObject.cs
@@
         public int Power;
+        public int CrystalTrapGroupId;
+        public bool CrystalTrapDetonated;
@@
                 case SpellEffect.CrystalHealingCircle2:
@@
                     break;
+                case SpellEffect.CrystalExplosiveTrap:
+                    {
+                        if (CrystalTrapDetonated || Owner is not PlayerObject player || ob?.Node == null || ob.Dead) return;
+                        if (!player.CanAttackTarget(ob)) return;
+
+                        CrystalTrapDetonated = true;
+                        player.MagicAttack(new List<MagicType> { MagicType.ExplosiveTrap }, ob, true, null, Power);
+
+                        // Crystal links the front/left/right pieces by cast id.
+                        foreach (SpellObject linked in player.SpellList.ToList())
+                        {
+                            if (linked.Effect != SpellEffect.CrystalExplosiveTrap || linked.CrystalTrapGroupId != CrystalTrapGroupId) continue;
+                            linked.CrystalTrapDetonated = true;
+                            linked.TickCount = Math.Min(linked.TickCount, 1);
+                            linked.TickTime = SEnvir.Now.AddSeconds(1);
+                        }
+                    }
+                    break;
+                case SpellEffect.CrystalNapalmShot:
+                case SpellEffect.CrystalNapalmShot2:
+                    {
+                        if (Owner is not PlayerObject player || ob?.Node == null || ob.Dead || !player.CanAttackTarget(ob)) return;
+                        MagicType type = Effect == SpellEffect.CrystalNapalmShot2 ? MagicType.NapalmShot2 : MagicType.NapalmShot;
+                        player.MagicAttack(new List<MagicType> { type }, ob, true, null, Power);
+                    }
+                    break;
                 case SpellEffect.PoisonousCloud:
