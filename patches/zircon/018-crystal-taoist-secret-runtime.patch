diff --git a/LibraryCore/Enum.cs b/LibraryCore/Enum.cs
--- a/LibraryCore/Enum.cs
+++ b/LibraryCore/Enum.cs
@@ -294,6 +294,8 @@ namespace Library
         EnergyShield = 314,
         Curse = 315,
         PetEnhancer = 316,
+        CrystalHealing2 = 317,
+        CrystalHealingCircle2 = 318,
@@
         CrystalPoisonCloud,
-        CrystalHealingCircle
+        CrystalHealingCircle,
+        CrystalHealingCircle2
     }
diff --git a/LibraryCore/Stat.cs b/LibraryCore/Stat.cs
--- a/LibraryCore/Stat.cs
+++ b/LibraryCore/Stat.cs
@@ -909,6 +909,9 @@ namespace Library
         [StatDescription(Title = "Energy Shield HP Gain", Format = "{0:+#0;-#0;#0}", Mode = StatType.Default, ServerOnly = true)]
         EnergyShieldHPGain = 11005,
+
+        [StatDescription(Title = "Healing Circle II Damage Reduction", Format = "{0:+#0%;-#0%;#0%}", Mode = StatType.Percent, ServerOnly = true)]
+        HealingCircle2ReductionPercent = 11006,
 
         [StatDescription(Title = "Duration", Mode = StatType.Time)]
diff --git a/ServerLibrary/Models/PlayerObject.cs b/ServerLibrary/Models/PlayerObject.cs
--- a/ServerLibrary/Models/PlayerObject.cs
+++ b/ServerLibrary/Models/PlayerObject.cs
@@
             if (Stats[Stat.EnergyShieldPercent] > 0 &&
                 SEnvir.Random.Next(100) < Stats[Stat.EnergyShieldPercent])
             {
                 ChangeHP(Stats[Stat.EnergyShieldHPGain]);
             }
+
+            // Jev Crystal-Monk HealingCircle2: its caster receives 20/30/40/50%
+            // incoming-damage reduction for the same lifetime as the field.
+            if (Stats[Stat.HealingCircle2ReductionPercent] > 0)
+            {
+                power -= power * Stats[Stat.HealingCircle2ReductionPercent] / 100;
+            }
 
             if (element != Element.None)
diff --git a/ServerLibrary/Models/SpellObject.cs b/ServerLibrary/Models/SpellObject.cs
--- a/ServerLibrary/Models/SpellObject.cs
+++ b/ServerLibrary/Models/SpellObject.cs
@@
                 case SpellEffect.CrystalHealingCircle:
                     {
@@
                     }
                     break;
+                case SpellEffect.CrystalHealingCircle2:
+                    {
+                        if (Owner is not PlayerObject player || ob?.Node == null || ob.Dead) return;
+
+                        // Jev explicitly excludes the caster from HealingCircle2 field ticks.
+                        if (ob == player) return;
+
+                        if (player.CanAttackTarget(ob))
+                        {
+                            player.MagicAttack(new List<MagicType> { MagicType.HealingCircle2 }, ob, true, null, Power);
+                            return;
+                        }
+
+                        if (!player.CanHelpTarget(ob) || ob.CurrentHP >= ob.Stats[Stat.Health]) return;
+
+                        BuffInfo existing = ob.Buffs.FirstOrDefault(x => x.Type == BuffType.CrystalHealing);
+                        if (existing == null)
+                        {
+                            ob.BuffAdd(
+                                BuffType.CrystalHealing,
+                                TimeSpan.MaxValue,
+                                new Stats { [Stat.Healing] = Power },
+                                false,
+                                false,
+                                TimeSpan.FromMilliseconds(600));
+                        }
+                        else
+                        {
+                            existing.Stats[Stat.Healing] += Power;
+                        }
+                    }
+                    break;
                 case SpellEffect.PoisonousCloud:
