# ORIGINS native maps

`0.map` is the ORIGINS Bichon Cataclysm hub, derived from pinned Zircon `Map/0.map`. It preserves the central Bichon town/wall context, adds a native-tile promenade, and replaces the remaining exterior with blocked native Zircon sea tiles. No painted/generated map art is used.
